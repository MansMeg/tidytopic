context("General TODO of the package")

test_that(desc="TODO",{
  skip_on_travis()
  expect_true(FALSE, info = "Fix dplyr an %>% so package work without first loading dplyr")
  expect_true(FALSE, info = "fix write_mallet_state()")
  expect_true(FALSE, info = "setup Travis")
  expect_true(FALSE, info = "setup Coverall")
  expect_true(FALSE, info = "fix github badges")
})

