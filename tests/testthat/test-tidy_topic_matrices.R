context("tidy_topic_matrices")

load(system.file("extdata/sotu50.Rdata", package = "tidytopics"))

test_that(desc="TODO",{
  skip_on_travis()
  expect_true(FALSE, info = "Implement test suite for topic matrices")
})

